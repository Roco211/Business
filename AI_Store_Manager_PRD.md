# AI数字店铺大管家 v2 PRD

版本：v2.0
日期：2026-04-03
阶段：可落地 MVP

## 0. 这版 v2 解决什么问题

这版不是继续把产品写得更大，而是把它收口成一版真的能做、能测、能上线的 MVP。

v1 最大的问题不是方向错，而是把三件事写在了一起：

- 产品愿景：AI 店铺操作系统
- MVP 范围：老板工作台 + 工作群 + 入库 + 查询 + 纠错
- 平台远景：人才市场、团队养成、插件扩展、通话协同

v2 的处理方式很简单：

- 保留最强的产品感：老板主要靠语音吩咐 AI 干活，拍照和单据识别负责补盲
- 保留最重要的工程底座：Tool、确认、审计、纠错
- 延后最重的系统能力：实时通话、人才市场、第三方扩展、多角色养成

一句话定义：

**AI数字店铺大管家 v2 是一个面向单店个体户、语音优先、多模态辅助的库存协同副驾。老板主要靠“说一句”发起操作，在语音不够时再“拍一张”或“拍一张单据”，系统完成入库、查询、识别辅助与账本回写，并保证所有关键改动可确认、可撤销、可追踪。**

## 1. 目标用户与核心场景

### 1.1 目标用户

- 下沉市场杂货店、百货店、便利店、五金杂铺的一人店主
- 年龄集中在 30-55 岁
- 以 Android 手机为主
- 习惯微信式交互，不习惯复杂 ERP

### 1.2 核心场景

- 收货时没空打字，希望直接说一句“今天进来 3 箱可乐”
- 巡店或理货时，希望直接问一句“红牛还剩多少”“这个还有没有库存”
- 遇到无码商品，希望拍一张照就能先识别、再确认
- 想拿起手机对着货架或商品拍一下，就能知道它是什么、还有多少库存
- 拿到纸质进货单时，希望拍一张单据就能先抽出商品、数量、价格，辅助入库
- AI 写错时，必须能手工改回来，而且改动能留痕

### 1.3 不作为 v2 目标用户的对象

- 多门店老板
- 有成熟 ERP 团队和收银系统集成需求的连锁商家
- 需要完整财务、税务、供应链自动下单的客户

## 2. 产品目标与成功标准

### 2.1 产品目标

用最少的界面和最少的学习成本，跑通以下闭环：

1. 老板优先通过语音发起入库、查询和追问
2. 语音不够时，老板可补一张商品照片、货架照片或进货单据
3. 系统把语音和图像理解成库存任务或查询任务
4. 系统要求确认必要字段
5. 系统写入库存事件或返回只读查询结果
6. 老板可以随时查、撤销、修正

### 2.2 MVP 成功标准

以下指标作为 v2 的内部验收目标：

- 短语音首响应时间 `< 3 秒`
- 语音发起的入库和查询必须能跑通核心演示链路
- 常见商品语音入库任务在 `60 秒内完成` 的比例达到 `80%+`
- 拍照识物、拍照查询、进货单 OCR 必须都能输出对老板有用的候选结果或结构化字段
- 所有库存写操作 `100%` 带审计记录
- 所有高风险或低置信动作 `100%` 进入确认态，不允许直接写库
- 人工纠错后的库存事件可追踪率 `100%`
- Top 100 常卖商品的入库人工纠错率在冷启动后两周内降到 `15% 以下`

## 3. v2 的产品原则

### 3.1 四个必须坚持的原则

- **先完成任务，再讲智能**
  - 用户要的是把货记进去，不是看一场 Agent 表演。
- **语音是主入口，拍照是关键辅助**
  - 这不是“有语音按钮的库存工具”，而是“老板先开口，系统再补图像理解”。
- **先做库存主链路，再扩展财务与市场**
  - 第一版必须围绕入库、查库存、补货提醒、人工纠错展开。
- **多模态不是点缀，是补齐信息缺口**
  - 无码商品、货架查询、纸质进货单都需要图像能力进入 MVP。
- **AI 负责理解，系统负责落账**
  - 模型不能直接修改库存，必须通过工具链和审计链。
- **所有错误都要有回退路径**
  - 用户可以确认、撤销、修改，系统必须留下原因与时间。

### 3.2 这版不追求什么

- 不追求“很多 AI 员工”
- 不追求完整实时通话协同
- 不追求开放插件市场
- 不追求全行业 ERP

## 4. MVP 范围

### 4.1 MVP 必做

- 工作台首页
- 工作群式任务交互
- 语音作为主入口
- 语音入库
- 语音查询库存
- 拍照识物入库
- 拍照查询商品/库存
- 进货单 OCR 辅助识别
- 库存查询
- 低库存提醒
- 手工账本纠错
- 审计时间线
- 店主规则：价格确认、低置信确认、新商品确认

### 4.2 MVP 暂不做

- 实时通话 / 屏幕共享 / 摄像头连续流
- 人才市场
- 团队管理页
- 独立财务员工“小丽”
- 赊账、退款冲销、完整利润分析
- 第三方插件
- 自动向供应商下单

### 4.3 MVP 的用户可见角色

v2 只保留两个用户可见角色：

- **小雅**
  - 唯一对话入口
  - 负责理解老板意图、解释原因、发起确认、返回结果
- **老李**
  - 库存执行角色
  - 负责入库、库存查询、补货提醒

说明：

- 查询能力仍存在，但由小雅统一呈现，不再单独做“小丽”页面和人格分工
- 多角色平台能力保留在技术设计里，但不作为 v2 主卖点

## 5. 信息架构

v2 的用户界面只保留 3 个一级页面：

1. **工作台**
   - 看今天的经营状态
   - 看待确认任务
   - 看库存告急
   - 有一个明显的语音主入口
   - 一键进入工作群或账本
2. **工作群**
   - 语音是主输入方式
   - 支持发文字
   - 支持拍商品图
   - 支持拍进货单
   - 接收结果卡、确认卡、查询结果、OCR 抽取结果
3. **账本**
   - 看当前库存
   - 看最近入库事件
   - 手工修正错误
   - 查看审计记录

## 6. 核心用户流程

### 6.1 流程 A：语音入库

场景：
老板说“今天进来 3 箱可乐”。

流程：

1. 小雅识别为库存增加任务
2. 系统匹配已有商品档案
3. 若数量、单位、商品信息完整，则生成确认卡
4. 老板确认后，工具写入 `InventoryEvent`
5. 群里返回结果卡，账本新增记录

验收：

- 用户不需要进入表单页
- 入库成功后能立刻看到新库存
- 结果卡必须带“查看账本”入口

### 6.2 流程 B：语音查询库存

场景：
老板说“红牛还剩多少？”或“这个还有没有库存？”

流程：

1. 小雅识别为只读查询任务
2. 若语音里商品名明确，则直接查库存
3. 若语音里说“这个”，系统提示老板补一张照片
4. 系统返回口语化结论 + 简短数字说明
5. 若库存过低，附带补货建议

验收：

- 查询不产生写操作
- 结论必须短，适合口播
- 若语音信息不足，系统能自然要求补图，而不是直接失败

### 6.3 流程 C：拍照识物入库

场景：
老板拍一张扳手照片，说“这个入一下库”。

流程：

1. 小雅识别为商品识别任务
2. 视觉工具返回商品候选与置信度
3. 若商品置信度低或为新商品，则进入待确认
4. 老板补充名称、数量、价格后确认
5. 系统写入商品档案与库存事件

验收：

- 低置信识别不允许直接写库
- 新商品第一次建档必须人工确认
- 系统要明确解释“为什么要你确认”

### 6.4 流程 D：拍照查询商品 / 库存

场景：
老板对着货架或单个商品拍一张照，说“这个还有没有库存？”

流程：

1. 小雅识别为拍照查询任务
2. 视觉工具识别商品候选
3. 系统调用库存查询工具
4. 返回“这是什么 + 还有多少 + 是否该补货”
5. 若识别不稳，系统返回候选而不是乱答

验收：

- 拍照查询不产生写操作
- 返回结果必须包含识别对象与库存结论
- 识别不稳时必须告诉老板“不确定”

### 6.5 流程 E：进货单 OCR 辅助识别

场景：
老板拍一张纸质进货单，说“帮我看一下这单货。”

流程：

1. 小雅识别为单据理解任务
2. OCR 工具抽取商品名、数量、单价、总额等字段
3. 系统把字段回写为结构化结果卡
4. 若字段置信度不足，要求老板确认或修改
5. 老板确认后，系统可继续生成待入库任务

验收：

- OCR 输出必须是对老板有用的字段，而不是原始文本
- 低置信字段必须单独标出
- OCR 可以辅助入库，但不能绕过确认直接批量写库

### 6.6 流程 F：人工纠错

场景：
老板发现 AI 把 2 箱写成了 3 箱。

流程：

1. 老板进入账本
2. 找到对应商品或最近事件
3. 手工修改数量并填写原因
4. 系统生成 `CorrectionEvent`
5. 审计时间线记录本次修正

验收：

- 不允许静默覆盖
- 必须记录修改前后值、原因、时间
- 修改完成后首页数据同步刷新

## 7. 功能需求

### 7.1 工作台

必须包含：

- 今日营业额
- 今日入库次数
- 语音入口状态
- 低库存商品数
- 待确认任务数
- 今日最近一条任务状态
- 一个明显的“按住说话”或“进入语音工作群”入口
- 一个明显的“去账本纠错”入口

工作台不做：

- 复杂利润分析
- 团队管理
- 人才市场入口
- 实时通话入口

### 7.2 工作群

必须包含：

- 语音输入入口，并且是最明显的主输入方式
- 文本输入
- 商品拍照入口
- 进货单拍照入口
- AI 回复气泡
- 结果卡
- 待确认卡
- 查询结果卡
- OCR 结构化结果卡

工作群不做：

- 完整附件系统
- 共享屏幕
- 连续通话界面
- 多角色同时上屏

### 7.3 账本

必须包含：

- 库存商品列表
- 最近库存事件
- 手工修正表单
- 审计时间线
- 纠错后的回执

账本不做：

- 完整财务报表
- 税务数据
- 供应商对账

## 8. AI 运行时与系统边界

### 8.1 v2 的真实实现方式

用户看见的是“小雅”和“老李”。

系统内部不要求一开始就实现复杂的真多智能体。v2 推荐采用：

- **1 个会话编排器**
  - 负责统一管理消息、任务、确认态和回执
- **1 套语音与多模态工具链**
  - 负责语音理解、商品识别、拍照查询、OCR 抽取、库存查询、库存写入、生成结果卡
- **1 套风控规则**
  - 负责低置信识别、新商品、价格异常、数量异常的确认逻辑

也就是说：

- **角色是前台体验**
- **运行时是后台编排**

### 8.2 核心运行链路

```text
老板输入
-> 小雅理解意图
-> 创建任务 TaskRun
-> 调用库存工具
-> 风控规则检查
-> 老板确认或拒绝
-> 写入 InventoryEvent / CorrectionEvent
-> 群里回结果
-> 首页与账本同步刷新
```

### 8.3 关键对象

- `ConversationSession`
  - 当前工作群会话
- `TaskRun`
  - 一次入库、查询或纠错任务
- `InventoryItem`
  - 商品档案
- `InventoryEvent`
  - 入库、出库、修正等库存事件
- `Alert`
  - 低库存提醒
- `AuditLog`
  - 所有关键动作的审计记录

## 9. 风控与确认规则

以下场景必须进入待确认：

- 新商品首次建档
- 商品识别置信度不足
- 数量单位不明确
- 价格为空
- 价格偏离历史区间过大
- 单次库存变化超过阈值

每次确认卡必须明确写出：

- 系统准备做什么
- 为什么不能直接做
- 用户确认后会改动什么数据

## 10. 冷启动策略

v2 落地最大的风险不是模型，而是商品档案冷启动。

因此第一版必须同时支持以下四种建档与辅助识别方式：

1. **已有商品匹配**
   - 条码或已有名称直接命中
2. **拍照建档**
   - 适合五金、散货、无码商品
3. **进货单 OCR 抽取**
   - 适合纸质单据先抽字段再确认
4. **手工补全**
   - 用户补名称、单位、价格后完成建档

建议首批优先做：

- 饮料
- 烟酒
- 泡面
- 日化
- 五金高频品类

## 11. 技术策略

### 11.1 技术原则

- 云端是账本真相源
- 端侧优先做低延迟理解，不直接做最终落账
- 写库动作必须经过服务端工具链和审计链

### 11.2 模型策略

截至 2026-04-03，v2 建议采用“云控 + 端侧加速”的混合策略：

- **端侧**
  - 在支持的 Android 设备上，可用 Gemma 4 E2B / E4B 作为低延迟理解与摘要能力的候选方案
  - 适合做意图初判、语音转写后清洗、简短查询回复、会话摘要压缩、轻量视觉预判
- **云端**
  - 负责商品识别复核、拍照查询、OCR 抽取、工具调用、风险判断、事件写入、审计留痕

注意：

- Gemma 端侧能力可以提升体验，但不是 v2 的硬依赖
- 不支持端侧模型的设备，仍然必须可以完整运行

### 11.3 供应商锁定策略

- ASR、视觉识别、LLM 编排都应可替换
- 不把产品逻辑绑死在单一模型厂商上
- 模型只负责理解和建议，业务规则必须写在系统里

## 12. 非功能要求

- 首响应快：短语音首响应 `< 3 秒`
- 语音主入口明显：用户第一次进入就能感知“可以直接说”
- 可解释：每个关键动作都要说清原因
- 可恢复：关键写操作必须能撤销或修正
- 可观测：每一步任务状态都能追踪
- 可审计：库存相关改动必须留痕
- 可降级：端侧能力不可用时，自动回落到云端流程
- 多模态可用：拍照识别、拍照查询、进货单 OCR 都必须有可运行的后备方案

## 13. 路线图

### Phase 0：v2 MVP

目标：

- 跑通“工作台 + 工作群 + 账本”三屏
- 跑通语音入库、语音查询、拍照建档、拍照查询、进货单 OCR、人工纠错六条主链路

### Phase 1：增强库存协同

加入：

- 更细的低库存策略
- 会话摘要续接
- 简单经营问答
- OCR 字段纠错体验优化

### Phase 2：重度协同模式

加入：

- 实时通话
- 摄像头连续流
- 屏幕共享核对
- 通话纪要

### Phase 3：平台化能力

加入：

- 团队管理
- 人才市场
- 官方能力包
- 第三方扩展机制

## 14. 最终结论

v2 不再把自己定义成“AI 店铺操作系统的完整版”，而是先把最值钱的一段闭环做深：

- 让老板可以不用表单，直接说和拍
- 让系统能稳稳地把库存记对
- 让每次错误都能改回来

如果这一段闭环跑通，后面的团队、通话、市场才有资格继续做大。
